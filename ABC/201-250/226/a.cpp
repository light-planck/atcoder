#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(),(x).end()
using ll = long long;

int main() {
  double x;
  cin >> x;
  double ans = x + 0.5;
  cout << (int)ans << endl;
  return 0;
}